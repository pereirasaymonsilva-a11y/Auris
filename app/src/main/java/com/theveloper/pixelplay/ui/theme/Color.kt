package com.theveloper.pixelplay.ui.theme

import androidx.compose.ui.graphics.Color

val PixelPlayPurpleDark = Color(0xFF1E1234)
val PixelPlayPurplePrimary = Color(0xFF6750A4)
val PixelPlayPink = Color(0xFFF06292)
val PixelPlayOrange = Color(0xFFFF8A65)
val PixelPlayLightPurple = Color(0xFFE1BEE7)
val PixelPlayWhite = Color(0xFFFFFFFF)
val PixelPlayBlack = Color(0xFF000000)
val PixelPlaySurface = Color(0xFF2A1F40)

// No arquivo Color.kt, substitua o bloco "Colores para tema claro (si se implementa)" por:

// Cores para tema claro (Amarelo Dourado e Preto)
val LightBackground = Color(0xFFFFFFFF)         // Fundo branco puro
val LightSurface = Color(0xFFFFF8E1)            // Superfície amarelo bem claro
val LightSurfaceVariant = Color(0xFFFFF3CD)     // Variação para cartões
val LightOnSurface = Color(0xFF000000)          // Texto preto sobre superfícies
val LightOnSurfaceVariant = Color(0xFF4D3E00)   // Texto de apoio em tom escuro
val LightPrimary = Color(0xFFFFC107)            // Amarelo dourado principal
val LightPrimaryContainer = Color(0xFFFFECB3)   // Container primário em amarelo suave
val LightOnPrimaryContainer = Color(0xFF000000) // Texto preto no container
val LightOutline = Color(0xFFB8860B)            // Contorno dourado escuro
